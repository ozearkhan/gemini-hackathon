"""Deterministic PDLC tools.

Following the FinSavant lesson: if a decision is fully specified by rules, encode
it in deterministic Python instead of asking the LLM to reason it out each time.
Faster, cheaper, and it cannot hallucinate.
"""
